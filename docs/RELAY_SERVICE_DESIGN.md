# Relay Service Design

Relay is reserved for future commercial routing. This stage defines request metadata and `RelayUsage`, but does not implement billing, accounts, quotas, or cloud service logic.

Relay must not expose upstream provider secrets to editor projects or cartridges.
